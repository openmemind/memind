package com.openmemind.ai.memory.evaluation.pipeline;

/**
 * Evaluation pipeline stage enumeration: ADD (write memory), SEARCH (retrieve), ANSWER (respond), EVALUATE (judge)
 *
 */
public enum Stage {
    ADD,
    SEARCH,
    ANSWER,
    EVALUATE
}
